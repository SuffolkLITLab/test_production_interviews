const create_feature_file = require('./create_feature_file');

// It may be better to work this into ALKiln proper. That way
// we can use all we've developed for it, like handling server
// reload failures.

create_feature_file();
